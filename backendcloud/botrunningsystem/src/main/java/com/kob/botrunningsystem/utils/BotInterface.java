package com.kob.botrunningsystem.utils;

/**
 * For users write their own bot
 * nextMove() method is the mandatory method in their bot
 */
public interface BotInterface {
  Integer nextMove(String input);
}
